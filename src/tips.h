#pragma once

#include <QStringList>

// User-facing suggestions are kept together so they remain easy to expand,
// review and translate independently from the dialog that displays them.
QStringList applicationTips();
